#include "main.h"

/**
 * swap_int - swaps the values of two integers
 * @a: first # to swap
 * @b: second # to swap
 *
 * Return: void
 */

void swap_int(int *a, int *b)

{
	int swp = *a;
	*a = *b;
	*b = c;
}
